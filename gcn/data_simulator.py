import sys
import pyrgg

def generate_data(rand_graph, bandwidth, nbr_nodes, nbr_of_features):
    pyrgg.json_maker(file_name="",)


def generate_features(nbr_of_features, adjacency):


def sample_data(val_pct, test_pct):


def save_to_file(objects,dataset_str):
